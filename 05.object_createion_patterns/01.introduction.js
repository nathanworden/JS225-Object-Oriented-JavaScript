// This lesson we will dive into object creation.

// JavaScript doesn't implement behavior sharing using class-based inheritance. Instead, it uses the object prototype to share properties. This distinction is of critical importance to understanding how JavaScript generates individual objects, and forms the basis of object-creation patterns that feature behavior sharing.