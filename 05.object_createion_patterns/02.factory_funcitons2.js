// function createPerson(firstName, lastName) {
//   var person = {};
//   person.firstName = firstName;
//   person.lastName = lastName || "";
//   person.fullName = funciton() {
//     return (this.firstName + ' ' + this.lastName).trim();
//   };

//   return person;
// }

// var john = createPerson('John', 'Doe');
// var jane = createPerson('Jane');

// john.fullName();
// jane.fullName();

// Two disadvantages:
// Methods are repeated in each new object. This is redundant and takes up a lot of space.
// We can't trace an object back to the fuction it was created with.

